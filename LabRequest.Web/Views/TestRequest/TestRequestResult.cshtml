@{
    ViewBag.Title = "لیست تستهای غیر روتین";
}
@if (ViewBag.reqno != null)
{ 
    <div class="row">
        <div class="ccol-sm-8 col-md-6 col-md-offset-3">
            <div class="panel panel-info">
                <div class="panel-heading">
                    <h4>
                        <i class="fa fa-lg fa-hand-pointer-o"></i>&#160شماره پیگیری درخواست ثبت شده</h4>
                </div>
                <div class="panel-body">
                    <div class="h2 text-center alert alert-success">
                        @ViewBag.reqno
                    </div>
                    <a href="@Url.Action("TestRequestAdd", "TestRequest")" class="pull-left btn btn-primary btn-sm">
                        <i class="fa fa-plus"></i>&#160صدور درخواست جدید</a>
                </div>
            </div>
        </div>
    </div>
}